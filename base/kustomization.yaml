---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mealie

commonLabels:
  app.kubernetes.io/name: mealie

resources:
  - pvc.yaml
  - deployment.yaml
  - svc.yaml
  - ingress.yaml

configMapGenerator:
  - name: mealie-config
    envs:
      - config.env
